const BASE_URL=import.meta.env.VITE_API_BASE_URL??'/api';
export interface MeResponse{uid:string;email:string;role:'ADMIN'|'MANAGER'|'STAFF'|'VIEWER';permissions:string[];status:'ACTIVE'}
export class ApiError extends Error{constructor(public status:number,public code:string){super(code)}}
async function request<T>(path:string,token:string,init?:RequestInit):Promise<T>{const response=await fetch(BASE_URL+path,{...init,headers:{'Content-Type':'application/json',Authorization:`Bearer ${token}`,...init?.headers}});const body=await response.json();if(!response.ok)throw new ApiError(response.status,body.error??'API_ERROR');return body}
export const api={me:(token:string)=>request<MeResponse>('/auth/me',token),list:(collection:string,token:string,params=new URLSearchParams())=>request(`/entities/${collection}?${params}`,token),create:(collection:string,data:Record<string,unknown>,token:string)=>request(`/entities/${collection}`,token,{method:'POST',body:JSON.stringify({data,operationId:crypto.randomUUID()})}),update:(collection:string,id:string,data:Record<string,unknown>,expectedVersion:number,token:string)=>request(`/entities/${collection}/${id}`,token,{method:'PATCH',body:JSON.stringify({data,expectedVersion,operationId:crypto.randomUUID()})})};
