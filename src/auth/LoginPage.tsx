import{useState,type FormEvent}from'react';import{ShieldCheck,Zap}from'lucide-react';import{useAuth}from'./AuthContext';
export function LoginPage({sessionError}:{sessionError?:string}){const{login}=useAuth();const[email,setEmail]=useState(''),[password,setPassword]=useState(''),[busy,setBusy]=useState(false),[error,setError]=useState(sessionError??'');async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError('');try{await login(email.trim(),password)}catch{setError('Email hoặc mật khẩu không đúng. Vui lòng thử lại.');setBusy(false)}}return <main className="login-page"><section className="login-card"><div className="login-logo"><Zap/> LƯỚI ĐIỆN 22kV</div><p className="eyebrow">HỆ THỐNG QUẢN LÝ</p><h1>Đăng nhập tài khoản</h1><p className="login-intro">Sử dụng tài khoản đã được quản trị viên phê duyệt.</p>{error&&<div className="login-error">{translateError(error)}</div>}<form onSubmit={submit}><label>Email<input type="email" autoComplete="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="ten@donvi.vn"/></label><label>Mật khẩu<input type="password" autoComplete="current-password" required minLength={6} value={password} onChange={e=>setPassword(e.target.value)} placeholder="••••••••"/></label><button disabled={busy}>{busy?'Đang đăng nhập…':'Đăng nhập'}</button></form><div className="login-secure"><ShieldCheck size={17}/> Phiên đăng nhập được Firebase bảo vệ</div></section></main>}
function translateError(error:string){if(error.includes('PENDING'))return'Tài khoản đang chờ quản trị viên phê duyệt.';if(error.includes('DISABLED')||error.includes('LOCKED'))return'Tài khoản đã bị khóa hoặc tạm ngưng.';if(error.includes('PROFILE_NOT_FOUND'))return'Tài khoản chưa có hồ sơ phân quyền.';return error}
