VITE_API_BASE_URL=/api
PORT=3001
FIREBASE_PROJECT_ID=your-project-id
# Local only: đường dẫn tuyệt đối đến service-account JSON.
# Production: dùng Application Default Credentials, không commit khóa.
GOOGLE_APPLICATION_CREDENTIALS=
